import React from 'react'
import Button from '../../components/Button'
import { getProductById } from "../../service"

export default function Homepage() {
    return <div>
        <Button type="button" onClick={}>Product1</Button>
    </div>
}